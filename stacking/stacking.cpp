/*
ID: jaising1
LANG: C++
TASK: angry
*/
#include <iostream>
#include <cstdio>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

FILE *fin;
fstream fout;
int main()
{
    freopen("stacking.in", "r", stdin);
    freopen("stacking.out", "w", stdout);

    fclose(stdin);
    fclose(stdout);
}