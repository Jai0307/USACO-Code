/*
ID: jaising1
LANG: C++
TASK: spaceout
*/
#include <iostream>
#include <cstdio>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

FILE* fin;
fstream fout;
#define infile "censor.in"
#define outfile "censor.out"
int main(){
    freopen("space.in", "r", stdin);
    freopen("space.out", "w", stdout);

    fclose(stdin);
    fclose(stdout);
}