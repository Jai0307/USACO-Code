/*
ID: jaising1
LANG: C++
TASK: badmilk
*/
#include <iostream>
#include <cstdio>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

struct Person
{
	bool sick;
	vector<vector<int>> timesNdrinks;
	int timeSick;
};


FILE* fin;
fstream fout;
int main(){
	freopen("badmilk.in", "r", stdin);
	freopen("badmilk.out", "w", stdout);
	int N,M,D,S;
	cin >> N >> M >> D >> S;

	


	fclose(stdin);
	fclose(stdout);
}